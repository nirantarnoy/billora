# 🔐 สรุปสถาปัตยกรรม Multi-Tenant OAuth สำหรับ Marketplace (Shopee, TikTok, Lazada)

เอกสารฉบับนี้สรุปโครงสร้างความปลอดภัยและการจัดการข้อมูลเชื่อมต่อของลูกค้าหลายราย (Multi-tenant) ในระบบ Billora

---

## 1. การแบ่งหน้าที่ของกุญแจ (Key Management)

ระบบถูกออกแบบให้แยก "กุญแจเจ้าของระบบ" ออกจาก "กุญแจของลูกค้า" เพื่อความสเกลและความปลอดภัย

### 🏠 อยู่ในไฟล์ `.env` (กุญแจแอป - Application Keys)
เป็นกุญแจที่คุณ (ผู้พัฒนา) ได้มาจาก Platform เพียงครั้งเดียว ใช้สำหรับระบุตัวตนของแอป Billora ต่อหน้า Shopee/TikTok
- **Partner ID / App Key**: ไอดีประจำตัวของแอป Billora
- **Partner Key / App Secret**: รหัสผ่านลับสุดยอดของแอป Billora (ใช้สำหรับคำนวณ Signature)
- **ENCRYPTION_KEY**: รหัสลับ 32 ตัวอักษรที่คุณเจนเอง ใช้เพื่อล็อก/ปลดล็อกข้อมูลของลูกค้าในฐานข้อมูล

### 🗄️ อยู่ในฐานข้อมูล (กุญแจร้านค้า - Tenant Tokens)
อยู่ในตาราง `marketplace_connections` เก็บกุญแจที่ได้รับหลังจากลูกค้าอนุญาตให้เข้าถึงข้อมูลร้านค้า
- **Access Token**: กุญแจชั่วคราวสำหรับดึงข้อมูลร้าน (ถูกเรียกจาก DB และ *ถอดรหัส* ก่อนใช้)
- **Refresh Token**: กุญแจชั่วคราวสำหรับขอ Access Token ใหม่เมื่อตัวเก่าหมดอายุ
- **Tenant ID**: ใช้ระบุว่ากุญแจดอกนี้เป็นของลูกค้าคนไหน

---

## 2. ขั้นตอนการเชื่อมต่อ (The OAuth Flow)

1.  **จุดเริ่มต้น**: ลูกค้ากดปุ่ม "เชื่อมต่อ Shopee" จากหน้า Dashboard ของเรา
2.  **ยืนยันตัวตน**: ระบบส่งลูกค้าไปยังหน้า Login ของ Shopee โดยบอกไอดีแอปของเราไปพร้อมกัน (`Partner ID` จาก `.env`)
3.  **การอนุญาต**: เมื่อลูกค้ากด "ตกลง" Shopee จะส่ง `Code` กลับมาที่ระบบของเรา
4.  **แลกเปลี่ยน**: ระบบนำ `Code` ไปแลกเป็น `Access Token` ของร้านลูกค้ารายนั้น
5.  **ความปลอดภัยขั้นสูง**: ระบบทำการ **เข้ารหัส (Encrypt)** Token นั้นด้วย `ENCRYPTION_KEY` แล้วบันทึกลงฐานข้อมูล

---

## 3. ทำไมต้องดึง Partner ID จาก `.env` แทนที่จะเก็บไว้ที่ลูกค้า?

- **การเป็นตัวแทนแอป**: Shopee คุยกับ Billora ในฐานะ "ผู้พัฒนา" (Developer) ดังนั้นเราต้องโชว์บัตรประจำตัวแอปของเราเสมอทุกครั้งที่คุยกับระบบเขา
- **ความง่ายสำหรับลูกค้า**: ลูกค้าไม่จำเป็นต้องสมัครเป็นนักพัฒนา (ซึ่งทำยากและรอนาน) ลูกค้าแค่มีบัญชีร้านค้าปกติก็เชื่อมต่อกับเราได้ทันที
- **กุญแจดอกเดียวไขได้ทุกร้าน**: เราใช้ใบอนุญาตชุดเดียวใน `.env` เพื่อดูแลลูกค้าหมื่นคนได้ โดยแยกเก็บใบอนุญาตรายคนไว้ในฐานข้อมูล

---

## 4. ความปลอดภัยในฐานข้อมูล (Encryption Strategy)

แม้ว่า Hacker จะขโมยไฟล์ฐานข้อมูลไปได้ (Database Breach) ข้อมูล Token ของลูกค้าก็จะยังปลอดภัยเพราะ:
- ข้อมูล Token ในตาราง `marketplace_connections` เป็นตัวอักษรสุ่มที่อ่านไม่ออก (Encrypted Text)
- Hacker ต้องใช้ `ENCRYPTION_KEY` จากไฟล์ `.env` ในเครื่อง Server ของเราเท่านั้นจึงจะไขรหัสได้

---

## 5. วิธีรันคำสั่งเช็กความถูกต้อง
หากมีการอัปเดตระบบฐานข้อมูล สามารถรันคำสั่งนี้เพื่อเช็กภาพรวมได้:
```bash
npm run setup-db
```

---
📍 **ไฟล์ที่เกี่ยวข้องในการจัดการความปลอดภัย:**
- `src/utils/crypto.js` (ฟังก์ชันเข้ารหัส/ถอดรหัส)
- `src/utils/ecommerce.js` (ตรรกะการคุยกับ API ต่างประเทศ)
- `src/controllers/ChannelController.js` (การจัดการเส้นทาง OAuth)
- `database/migrations/021_create_marketplace_connections_table.sql` (โครงสร้างตารางเก็บข้อมูล)

---

## 6. สิ่งที่เจ้าของระบบ (Developer) ต้องเตรียม

คุณต้องสมัครบัญชีผู้พัฒนาซอฟต์แวร์ (Developer Account) กับแต่ละ Platform เพื่อขอรับกุญแจหลัก (App Key / Secret) มาใส่ใน `.env`

| Platform | เว็บไซต์สำหรับการสมัคร | สิ่งที่จะได้รับมาใส่ใน `.env` |
| :--- | :--- | :--- |
| **Shopee** | [Shopee Open Platform](https://open.shopee.com/) | Partner ID & Partner Key |
| **TikTok** | [TikTok Shop Open Platform](https://services.tiktokshops.com/open/) | App Key & App Secret |
| **Lazada** | [Lazada Open Platform](https://open.lazada.com/) | App Key & App Secret |

---

## 7. ความเหมือนและความต่างระหว่าง Platform

ระบบ Billora ถูกออกแบบโดยใช้มาตรฐาน **OAuth 2.0** ซึ่งเป็นมาตรฐานสากลที่ทุกเจ้าใช้ร่วมกัน

- **สิ่งที่เหมือนกัน (90%)**: ขั้นตอนการ Redirection หน้าขอสิทธิ์, การรับ Code กลับมา, และการแลกเปลี่ยน Code เป็น Token รวมถึงระบบการใช้ Refresh Token เพื่อขอ Access Token ใหม่
- **สิ่งที่ต่างกันเล็กน้อย**:
    - **Signature Logic**: วิธีการเข้ารหัสข้อมูลที่ส่งไปคุยกับ API (เช่น Lazada จะมีความซับซ้อนกว่าเจ้าอื่นเล็กน้อย)
    - **Terminology**: ชื่อเรียกใน API เช่น Shopee ใช้ `order_sn`, TikTok ใช้ `order_id`
    - **Token Expiry**: ระยะเวลาหมดอายุของกุญแจที่มีความยาวไม่เท่ากันในแต่ละค่าย

**ข้อแนะนำ**: ให้เริ่มพัฒนาและทดสอบจาก **Shopee** เป็นอันดับแรก เนื่องจากมีโครงสร้างที่เข้าใจง่ายที่สุด จากนั้นจึงค่อยขยายผลไปยัง TikTok และ Lazada โดยใช้โครงสร้างฐานข้อมูลตาราง `marketplace_connections` ที่เราเตรียมไว้รองรับได้ทันที
