<div class="p-10 space-y-10">
    <!-- Page Header -->
    <div class="flex flex-col md:flex-row md:items-center justify-between gap-6">
        <div>
            <h2 class="text-3xl font-bold text-slate-800">สำรองและคืนค่าข้อมูล</h2>
            <p class="text-slate-500 mt-1">จัดการไฟล์สำรองฐานข้อมูล (MySQL)</p>
        </div>
        <form action="/admin/backup/create" method="POST">
            <input type="hidden" name="_csrf" value="<%= typeof _csrf !== 'undefined' ? _csrf : '' %>">
            <button type="submit"
                class="w-full md:w-auto bg-emerald-600 hover:bg-emerald-700 text-white px-8 py-3 rounded-2xl font-bold shadow-xl shadow-emerald-600/20 transition-all flex items-center justify-center gap-2">
                <i class="fas fa-plus"></i> สร้างจุดสำรองข้อมูลใหม่
            </button>
        </form>
    </div>

    <% if (success) { %>
        <div
            class="bg-emerald-50 border border-emerald-200 text-emerald-700 px-4 py-3 rounded-xl mb-6 flex items-center gap-3 font-bold text-sm">
            <i class="fas fa-check-circle"></i>
            <span>
                <%= success %>
            </span>
        </div>
        <% } %>

            <% if (error) { %>
                <div
                    class="bg-red-50 border border-red-200 text-red-700 px-4 py-3 rounded-xl mb-6 flex items-center gap-3 font-bold text-sm">
                    <i class="fas fa-exclamation-circle"></i>
                    <span>
                        <%= error %>
                    </span>
                </div>
                <% } %>

                    <!-- Backup List -->
                    <div class="bg-white rounded-[32px] shadow-sm border border-slate-100 overflow-hidden">
                        <div class="overflow-x-auto">
                            <table class="w-full text-left">
                                <thead class="bg-slate-50 border-b border-slate-100">
                                    <tr>
                                        <th
                                            class="px-8 py-5 text-[10px] font-bold text-slate-400 uppercase tracking-widest">
                                            ชื่อไฟล์</th>
                                        <th
                                            class="px-6 py-4 text-[10px] font-bold text-slate-400 uppercase tracking-widest">
                                            ขนาด</th>
                                        <th
                                            class="px-6 py-4 text-[10px] font-bold text-slate-400 uppercase tracking-widest">
                                            วันที่สร้าง</th>
                                        <th
                                            class="px-8 py-5 text-[10px] font-bold text-slate-400 uppercase tracking-widest text-right">
                                            จัดการ</th>
                                    </tr>
                                </thead>
                                <tbody class="divide-y divide-slate-50">
                                    <% if (backups.length===0) { %>
                                        <tr>
                                            <td colspan="4" class="px-8 py-24 text-center text-slate-400">
                                                <i class="fas fa-database text-4xl mb-3 block"></i>
                                                ยังไม่มีการสำรองข้อมูล
                                            </td>
                                        </tr>
                                        <% } else { %>
                                            <% backups.forEach(file=> { %>
                                                <tr class="hover:bg-slate-50 transition-colors">
                                                    <td class="px-8 py-5">
                                                        <div class="flex items-center gap-3">
                                                            <div
                                                                class="w-10 h-10 bg-slate-100 rounded-xl flex items-center justify-center text-slate-500 border border-slate-200">
                                                                <i class="fas fa-file-code"></i>
                                                            </div>
                                                            <span class="font-bold text-slate-700">
                                                                <%= file.name %>
                                                            </span>
                                                        </div>
                                                    </td>
                                                    <td class="px-6 py-4 text-slate-500 font-medium">
                                                        <%= file.size %>
                                                    </td>
                                                    <td class="px-6 py-4 text-slate-500 font-medium">
                                                        <%= new Date(file.date).toLocaleString('th-TH') %>
                                                    </td>
                                                    <td class="px-8 py-5 text-right">
                                                        <div class="flex justify-end gap-3">
                                                            <button onclick="confirmRestore('<%= file.name %>')"
                                                                class="p-2 text-indigo-600 hover:bg-indigo-50 rounded-xl transition-colors"
                                                                title="Restore">
                                                                <i class="fas fa-undo"></i>
                                                            </button>
                                                            <button onclick="confirmDelete('<%= file.name %>')"
                                                                class="p-2 text-rose-500 hover:bg-rose-50 rounded-xl transition-colors"
                                                                title="Delete">
                                                                <i class="fas fa-trash-alt"></i>
                                                            </button>
                                                        </div>
                                                    </td>
                                                </tr>
                                                <% }) %>
                                                    <% } %>
                                </tbody>
                            </table>
                        </div>
                    </div>

                    <script>
                        function confirmRestore(fileName) {
                            Swal.fire({
                                title: 'คืนค่าข้อมูล?',
                                html: `
                                    <div class="text-left space-y-3">
                                        <p class="text-slate-600">คุณต้องการคืนค่าข้อมูลจากไฟล์:</p>
                                        <div class="bg-slate-100 p-3 rounded-xl">
                                            <p class="font-bold text-slate-800 break-all">
                                                <i class="fas fa-file-code text-indigo-600"></i> ${fileName}
                                            </p>
                                        </div>
                                        <div class="bg-red-50 border border-red-200 p-3 rounded-xl">
                                            <p class="text-red-700 font-bold text-sm">
                                                <i class="fas fa-exclamation-triangle"></i> คำเตือน
                                            </p>
                                            <p class="text-red-600 text-sm mt-1">
                                                การคืนค่าจะทำการลบข้อมูลปัจจุบันทั้งหมดและแทนที่ด้วยข้อมูลจากไฟล์สำรอง
                                            </p>
                                        </div>
                                    </div>
                                `,
                                icon: 'warning',
                                showCancelButton: true,
                                confirmButtonColor: '#6366f1',
                                cancelButtonColor: '#94a3b8',
                                confirmButtonText: '<i class="fas fa-undo"></i> ยืนยัน คืนค่าข้อมูล',
                                cancelButtonText: 'ยกเลิก',
                                customClass: {
                                    popup: 'rounded-3xl',
                                    confirmButton: 'font-bold px-6',
                                    cancelButton: 'font-bold px-6'
                                },
                                width: '600px'
                            }).then((result) => {
                                if (result.isConfirmed) {
                                    // Create and submit form
                                    const form = document.createElement('form');
                                    form.method = 'POST';
                                    form.action = '/admin/backup/restore';

                                    const csrfInput = document.createElement('input');
                                    csrfInput.type = 'hidden';
                                    csrfInput.name = '_csrf';
                                    csrfInput.value = '<%= typeof _csrf !== "undefined" ? _csrf : "" %>';

                                    const fileInput = document.createElement('input');
                                    fileInput.type = 'hidden';
                                    fileInput.name = 'fileName';
                                    fileInput.value = fileName;

                                    form.appendChild(csrfInput);
                                    form.appendChild(fileInput);
                                    document.body.appendChild(form);

                                    // Show loading
                                    Swal.fire({
                                        title: 'กำลังคืนค่าข้อมูล...',
                                        html: 'กรุณารอสักครู่',
                                        allowOutsideClick: false,
                                        didOpen: () => {
                                            Swal.showLoading();
                                        }
                                    });

                                    form.submit();
                                }
                            });
                        }

                        function confirmDelete(fileName) {
                            Swal.fire({
                                title: 'ลบไฟล์สำรอง?',
                                html: `
                                    <div class="text-left space-y-3">
                                        <p class="text-slate-600">คุณต้องการลบไฟล์สำรองนี้:</p>
                                        <div class="bg-slate-100 p-3 rounded-xl">
                                            <p class="font-bold text-slate-800 break-all">
                                                <i class="fas fa-file-code text-rose-600"></i> ${fileName}
                                            </p>
                                        </div>
                                        <p class="text-slate-500 text-sm">
                                            <i class="fas fa-info-circle"></i> การลบไฟล์สำรองจะไม่สามารถกู้คืนได้
                                        </p>
                                    </div>
                                `,
                                icon: 'question',
                                showCancelButton: true,
                                confirmButtonColor: '#ef4444',
                                cancelButtonColor: '#94a3b8',
                                confirmButtonText: '<i class="fas fa-trash-alt"></i> ยืนยัน ลบเลย',
                                cancelButtonText: 'ยกเลิก',
                                customClass: {
                                    popup: 'rounded-3xl',
                                    confirmButton: 'font-bold px-6',
                                    cancelButton: 'font-bold px-6'
                                },
                                width: '500px'
                            }).then((result) => {
                                if (result.isConfirmed) {
                                    // Create and submit form
                                    const form = document.createElement('form');
                                    form.method = 'POST';
                                    form.action = '/admin/backup/delete';

                                    const csrfInput = document.createElement('input');
                                    csrfInput.type = 'hidden';
                                    csrfInput.name = '_csrf';
                                    csrfInput.value = '<%= typeof _csrf !== "undefined" ? _csrf : "" %>';

                                    const fileInput = document.createElement('input');
                                    fileInput.type = 'hidden';
                                    fileInput.name = 'fileName';
                                    fileInput.value = fileName;

                                    form.appendChild(csrfInput);
                                    form.appendChild(fileInput);
                                    document.body.appendChild(form);
                                    form.submit();
                                }
                            });
                        }
                    </script>

                    <div class="mt-8 bg-amber-50 rounded-[32px] p-8 border border-amber-100">
                        <h4
                            class="text-amber-800 font-black flex items-center gap-2 mb-3 uppercase tracking-wider text-sm">
                            <i class="fas fa-exclamation-triangle"></i> ข้อควรระวัง
                        </h4>
                        <ul class="text-amber-700 text-sm space-y-2 list-disc ml-5 font-medium">
                            <li>การคืนค่า (Restore)
                                จะทำการลบข้อมูลปัจจุบันและแทนที่ด้วยข้อมูลจากไฟล์สำรอง</li>
                            <li>ควรสร้างจุดสำรองข้อมูล (Backup)
                                ไว้ก่อนทำการเปลี่ยนแปลงโครงสร้างฐานข้อมูล</li>
                            <li>ไฟล์สำรองจะถูกเก็บไว้ที่โฟลเดอร์ <code
                                    class="bg-amber-100 px-2 py-0.5 rounded-lg text-amber-900 border border-amber-200">/backups</code>
                                ใน Server
                            </li>
                        </ul>
                    </div>
</div>