<div class="p-10">
    <!-- Page Header -->
    <div class="flex flex-col md:flex-row md:items-center justify-between gap-4 mb-10">
        <div>
            <h2 class="text-3xl font-bold text-slate-800">คำสั่งซื้อทั้งหมด (Omni-Channel Orders)</h2>
            <p class="text-slate-500 mt-1">รวมคำสั่งซื้อจาก Shopee, Lazada, TikTok และหน้าร้าน</p>
        </div>
        <div class="flex gap-2">
            <button
                class="bg-white text-slate-600 px-6 py-3 rounded-xl font-bold border border-slate-200 hover:bg-slate-50 transition-all flex items-center gap-2 text-sm">
                <i class="fas fa-download"></i> Export
            </button>
            <button
                class="bg-emerald-600 hover:bg-emerald-700 text-white px-6 py-3 rounded-xl font-bold shadow-lg shadow-emerald-600/20 transition-all flex items-center gap-2 text-sm">
                <i class="fas fa-sync-alt"></i> ดึงข้อมูลล่าสุด
            </button>
        </div>
    </div>

    <!-- Filters Section -->
    <div class="bg-white p-6 rounded-[32px] shadow-sm border border-slate-100 mb-8">
        <form action="/orders" method="GET" class="grid grid-cols-1 md:grid-cols-5 gap-6 items-end">
            <div class="md:col-span-1">
                <label
                    class="block text-[10px] font-bold text-slate-400 uppercase tracking-widest mb-1.5 ml-2">ช่องทาง</label>
                <select name="channel"
                    class="w-full bg-slate-50 border border-slate-100 rounded-xl px-4 py-2.5 focus:ring-2 focus:ring-emerald-500 outline-none transition-all text-sm font-bold">
                    <option value="">ทั้งหมด</option>
                    <option value="SHOPEE" <%=filter.channel=='SHOPEE' ? 'selected' : '' %>>Shopee</option>
                    <option value="LAZADA" <%=filter.channel=='LAZADA' ? 'selected' : '' %>>Lazada</option>
                    <option value="TIKTOK" <%=filter.channel=='TIKTOK' ? 'selected' : '' %>>TikTok</option>
                    <option value="DIRECT" <%=filter.channel=='DIRECT' ? 'selected' : '' %>>หน้าร้าน (Direct)
                    </option>
                </select>
            </div>
            <div class="md:col-span-1">
                <label
                    class="block text-[10px] font-bold text-slate-400 uppercase tracking-widest mb-1.5 ml-2">สถานะ</label>
                <select name="status"
                    class="w-full bg-slate-50 border border-slate-100 rounded-xl px-4 py-2.5 focus:ring-2 focus:ring-emerald-500 outline-none transition-all text-sm font-bold">
                    <option value="">ทั้งหมด</option>
                    <option value="PENDING" <%=filter.status=='PENDING' ? 'selected' : '' %>>รอจัดของ</option>
                    <option value="PICKING" <%=filter.status=='PICKING' ? 'selected' : '' %>>กำลังเบิก</option>
                    <option value="SHIPPED" <%=filter.status=='SHIPPED' ? 'selected' : '' %>>ส่งแล้ว</option>
                    <option value="CANCELLED" <%=filter.status=='CANCELLED' ? 'selected' : '' %>>ยกเลิก</option>
                </select>
            </div>
            <div class="md:col-span-1">
                <label
                    class="block text-[10px] font-bold text-slate-400 uppercase tracking-widest mb-1.5 ml-2">เริ่มวันที่</label>
                <input type="date" name="startDate" value="<%= filter.startDate %>"
                    class="w-full bg-slate-50 border border-slate-100 rounded-xl px-4 py-2.5 focus:ring-2 focus:ring-emerald-500 outline-none transition-all text-sm font-bold uppercase">
            </div>
            <div class="md:col-span-1">
                <label
                    class="block text-[10px] font-bold text-slate-400 uppercase tracking-widest mb-1.5 ml-2">ถึงวันที่</label>
                <input type="date" name="endDate" value="<%= filter.endDate %>"
                    class="w-full bg-slate-50 border border-slate-100 rounded-xl px-4 py-2.5 focus:ring-2 focus:ring-emerald-500 outline-none transition-all text-sm font-bold uppercase">
            </div>
            <div class="flex gap-2">
                <button type="submit"
                    class="flex-1 bg-slate-800 hover:bg-slate-900 text-white px-6 py-2.5 rounded-xl font-bold transition-all text-sm">
                    ค้นหา
                </button>
                <a href="/orders"
                    class="bg-slate-100 p-2.5 rounded-xl text-slate-500 hover:bg-slate-200 transition-all">
                    <i class="fas fa-undo"></i>
                </a>
            </div>
        </form>
    </div>

    <!-- Order Table -->
    <div class="bg-white rounded-[32px] shadow-sm border border-slate-100 overflow-hidden">
        <div class="overflow-x-auto">
            <table class="w-full text-left">
                <thead class="bg-slate-50 border-b border-slate-100">
                    <tr>
                        <th class="px-8 py-5 text-[10px] font-bold text-slate-400 uppercase tracking-widest">
                            วันที่ดึงข้อมูล</th>
                        <th class="px-6 py-5 text-[10px] font-bold text-slate-400 uppercase tracking-widest">
                            เลขที่ Order / ช่องทาง</th>
                        <th class="px-6 py-5 text-[10px] font-bold text-slate-400 uppercase tracking-widest">
                            รายการสินค้า</th>
                        <th class="px-6 py-5 text-[10px] font-bold text-slate-400 uppercase tracking-widest text-right">
                            ยอดเงินเป้าหมาย</th>
                        <th
                            class="px-6 py-5 text-[10px] font-bold text-slate-400 uppercase tracking-widest text-center">
                            สถานะ</th>
                        <th class="px-8 py-5 text-[10px] font-bold text-slate-400 uppercase tracking-widest text-right">
                            จัดการ</th>
                    </tr>
                </thead>
                <tbody class="divide-y divide-slate-50">
                    <% if (orders.length===0) { %>
                        <tr>
                            <td colspan="6" class="px-8 py-24 text-center text-slate-400">
                                <div class="flex flex-col items-center gap-3">
                                    <div
                                        class="w-16 h-16 bg-slate-50 rounded-full flex items-center justify-center text-2xl text-slate-200">
                                        <i class="fas fa-shopping-basket"></i>
                                    </div>
                                    <p class="font-bold">ไม่พบรายการคำสั่งซื้อตามเงื่อนไขที่ระบุ</p>
                                </div>
                            </td>
                        </tr>
                        <% } else { %>
                            <% orders.forEach(order=> { %>
                                <tr class="hover:bg-slate-50/50 transition-all group">
                                    <td class="px-8 py-5">
                                        <div class="text-sm font-bold text-slate-700">
                                            <%= new Date(order.created_at).toLocaleDateString('th-TH') %>
                                        </div>
                                        <div class="text-[10px] text-slate-400 font-bold">
                                            <%= new Date(order.created_at).toLocaleTimeString('th-TH') %>
                                        </div>
                                    </td>
                                    <td class="px-6 py-5">
                                        <div class="text-sm font-black text-slate-800">#<%= order.source_order_id %>
                                        </div>
                                        <div class="flex items-center gap-1.5 mt-1.5">
                                            <% if(order.source_platform==='SHOPEE' ) { %>
                                                <div
                                                    class="w-4 h-4 bg-[#ff4a00] rounded-[4px] flex items-center justify-center text-[8px] text-white font-black">
                                                    S</div>
                                                <% } else if(order.source_platform==='LAZADA' ) { %>
                                                    <div
                                                        class="w-4 h-4 bg-[#000080] rounded-[4px] flex items-center justify-center text-[8px] text-white font-black">
                                                        L</div>
                                                    <% } else if(order.source_platform==='TIKTOK' ) { %>
                                                        <div
                                                            class="w-4 h-4 bg-black rounded-[4px] flex items-center justify-center text-[8px] text-white font-black">
                                                            T</div>
                                                        <% } else { %>
                                                            <div
                                                                class="w-4 h-4 bg-slate-400 rounded-[4px] flex items-center justify-center text-[8px] text-white font-black">
                                                                D</div>
                                                            <% } %>
                                                                <span
                                                                    class="text-[10px] font-black text-slate-500 uppercase tracking-widest">
                                                                    <%= order.source_platform %>
                                                                </span>
                                        </div>
                                    </td>
                                    <td class="px-6 py-5">
                                        <div class="text-xs text-slate-600 space-y-1 font-bold">
                                            <% if (order.items_list) { %>
                                                <% JSON.parse('[' + order.items_list + ']' ).forEach(item=> { %>
                                                    <div class="flex justify-between gap-4">
                                                        <span class="truncate max-w-[150px]">
                                                            <%= item.name %>
                                                        </span>
                                                        <span class="font-black text-slate-400 truncate">x
                                                            <%= item.quantity %>
                                                        </span>
                                                    </div>
                                                    <% }) %>
                                                        <% } else { %>
                                                            <span
                                                                class="italic text-slate-300">ไม่พบข้อมูลรายสินค้า</span>
                                                            <% } %>
                                        </div>
                                    </td>
                                    <td class="px-6 py-5 text-right font-black text-slate-800">
                                        ฿<%= (order.total_amount || 0).toLocaleString(undefined, {minimumFractionDigits:
                                            2}) %>
                                    </td>
                                    <td class="px-6 py-5 text-center">
                                        <span class="px-3 py-1 rounded-full text-[10px] font-black uppercase tracking-widest
                                                        <%= order.status === 'SHIPPED' ? 'bg-emerald-100 text-emerald-600' : 
                                                            order.status === 'CANCELLED' ? 'bg-rose-100 text-rose-600' :
                                                            order.status === 'PICKING' ? 'bg-blue-100 text-blue-600' :
                                                            'bg-slate-100 text-slate-500' %>">
                                            <%= order.status %>
                                        </span>
                                        <% if (order.payment_slip_id) { %>
                                            <div
                                                class="flex items-center justify-center gap-1 text-[9px] text-emerald-500 font-bold mt-1.5 uppercase">
                                                <i class="fas fa-check-circle"></i> ชำระแล้ว
                                            </div>
                                            <% } %>
                                    </td>
                                    <td class="px-8 py-5 text-right">
                                        <div
                                            class="flex justify-end gap-3 opacity-0 group-hover:opacity-100 transition-opacity">
                                            <button
                                                class="w-8 h-8 rounded-lg bg-slate-50 text-slate-400 hover:bg-emerald-50 hover:text-emerald-600 transition-all border border-slate-100">
                                                <i class="fas fa-eye text-xs"></i>
                                            </button>
                                            <button
                                                class="w-8 h-8 rounded-lg bg-slate-50 text-slate-400 hover:bg-blue-50 hover:text-blue-600 transition-all border border-slate-100">
                                                <i class="fas fa-print text-xs"></i>
                                            </button>
                                        </div>
                                    </td>
                                </tr>
                                <% }) %>
                                    <% } %>
                </tbody>
            </table>
        </div>

        <!-- Pagination Placeholder -->
        <div class="px-8 py-5 bg-slate-50/50 border-t border-slate-100 flex justify-between items-center">
            <p class="text-[10px] font-bold text-slate-400 uppercase tracking-widest">แสดง <%= orders.length %>
                    รายการล่าสุด</p>
            <div class="flex gap-2">
                <button
                    class="px-4 py-2 bg-white border border-slate-200 rounded-xl text-[10px] font-bold text-slate-400 uppercase tracking-widest cursor-not-allowed">Previous</button>
                <button
                    class="w-10 h-10 bg-emerald-600 border border-emerald-600 rounded-xl text-xs font-black text-white shadow-lg shadow-emerald-600/20">1</button>
                <button
                    class="px-4 py-2 bg-white border border-slate-200 rounded-xl text-[10px] font-bold text-slate-600 uppercase tracking-widest hover:bg-slate-50 transition-all">Next</button>
            </div>
        </div>
    </div>