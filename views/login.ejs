<!DOCTYPE html>
<html lang="th">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Bill Pro | เข้าสู่ระบบ</title>
    <script src="/js/tailwind.min.js"></script>
    <link href="/css/all.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Anuphan:wght@300;400;600;700&family=Inter:wght@300;400;500;600;700&family=Prompt:wght@300;400;500;600;700&display=swap');

        :root {
            --primary: #6366f1;
            --primary-dark: #4f46e5;
            --primary-light: #e0e7ff;
            --white: #ffffff;
        }

        body {
            font-family: 'Inter', 'Prompt', 'Anuphan', sans-serif;
            background-color: var(--white);
            background-image:
                radial-gradient(circle at top right, var(--primary-light) 0%, transparent 40%),
                radial-gradient(circle at bottom left, #e0f2fe 0%, transparent 40%);
            background-attachment: fixed;
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            position: relative;
            overflow: hidden;
        }

        .bg-blob {
            position: absolute;
            width: 500px;
            height: 500px;
            background: var(--primary-light);
            filter: blur(80px);
            border-radius: 50%;
            z-index: -1;
            opacity: 0.5;
            animation: float 20s infinite alternate;
        }

        .blob-1 {
            top: -100px;
            right: -100px;
        }

        .blob-2 {
            bottom: -100px;
            left: -100px;
            background: #e0f2fe;
            animation-delay: -5s;
        }

        @keyframes float {
            0% {
                transform: translate(0, 0) scale(1);
            }

            100% {
                transform: translate(50px, 50px) scale(1.1);
            }
        }

        .login-card {
            background: rgba(255, 255, 255, 0.8);
            backdrop-filter: blur(20px);
            border: 1px solid rgba(255, 255, 255, 0.4);
            box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.1);
            animation: cardAppear 0.8s cubic-bezier(0.16, 1, 0.3, 1);
        }

        @keyframes cardAppear {
            0% {
                opacity: 0;
                transform: translateY(20px);
            }

            100% {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .input-error {
            border-color: #ef4444 !important;
            background-color: #fef2f2 !important;
        }

        .error-hint {
            color: #ef4444;
            font-size: 0.75rem;
            margin-top: 0.25rem;
            font-weight: 500;
        }
    </style>
</head>

<body class="p-4">
    <div class="bg-blob blob-1"></div>
    <div class="bg-blob blob-2"></div>

    <div class="w-full max-w-md relative z-10">
        <div class="login-card rounded-3xl p-10">
            <div class="flex flex-col items-center mb-10">
                <div
                    class="w-16 h-16 bg-indigo-600 rounded-2xl flex items-center justify-center shadow-lg shadow-indigo-500/50 mb-4">
                    <i class="fas fa-file-invoice text-white text-3xl"></i>
                </div>
                <h1 class="text-3xl font-bold text-slate-800">GoodOper<span class="text-indigo-500">Pro</span></h1>
                <p class="text-slate-500 mt-2">กรุณาเข้าสู่ระบบเพื่อใช้งาน</p>
            </div>

            <form id="loginForm" action="/login" method="POST" class="space-y-6" novalidate>
                <input type="hidden" name="_csrf" value="<%= typeof _csrf !== 'undefined' ? _csrf : '' %>">
                <div>
                    <label class="block text-sm font-semibold text-slate-700 mb-2">ชื่อผู้ใช้</label>
                    <div class="relative">
                        <i class="fas fa-user absolute left-4 top-1/2 -translate-y-1/2 text-slate-400"></i>
                        <input type="text" name="username" id="username"
                            class="w-full pl-12 pr-4 py-3 bg-slate-50 border border-slate-200 rounded-xl focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500 outline-none transition-all"
                            placeholder="ใส่ชื่อผู้ใช้ของคุณ">
                    </div>
                    <div id="username-error" class="error-hint hidden">กรุณากรอกชื่อผู้ใช้</div>
                </div>

                <div>
                    <label class="block text-sm font-semibold text-slate-700 mb-2">รหัสผ่าน</label>
                    <div class="relative">
                        <i class="fas fa-lock absolute left-4 top-1/2 -translate-y-1/2 text-slate-400"></i>
                        <input type="password" name="password" id="password"
                            class="w-full pl-12 pr-4 py-3 bg-slate-50 border border-slate-200 rounded-xl focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500 outline-none transition-all"
                            placeholder="ใส่รหัสผ่านของคุณ">
                    </div>
                    <div id="password-error" class="error-hint hidden">กรุณากรอกรหัสผ่าน</div>
                </div>

                <div class="flex items-center justify-between text-sm">
                    <label class="flex items-center gap-2 text-slate-600 cursor-pointer">
                        <input type="checkbox" class="rounded border-slate-300 text-indigo-600 focus:ring-indigo-500">
                        จดจำฉัน
                    </label>
                    <a href="#" class="text-indigo-600 font-semibold hover:underline">ลืมรหัสผ่าน?</a>
                </div>

                <button type="submit"
                    class="w-full bg-indigo-600 hover:bg-indigo-700 text-white py-4 rounded-xl font-bold shadow-lg shadow-indigo-600/30 transition-all hover:scale-[1.02] transform active:scale-95">
                    เข้าสู่ระบบ
                </button>
            </form>

            <div class="mt-8 pt-8 border-t border-slate-100 text-center">
                <p class="text-slate-500 text-sm">ยังไม่มีบัญชี?
                    <a href="/register" class="text-indigo-600 font-bold hover:underline">ลงทะเบียนองค์กร</a>
                </p>
            </div>
        </div>
    </div>

    <script>
        const Toast = Swal.mixin({
            toast: true,
            position: "top-end",
            showConfirmButton: false,
            timer: 3000,
            timerProgressBar: true,
            didOpen: (toast) => {
                toast.onmouseenter = Swal.stopTimer;
                toast.onmouseleave = Swal.resumeTimer;
            }
        });

        // ดูว่ามี error จาก server ไหม
        <% if (error) { %>
            Toast.fire({
                icon: "error",
                title: "<%= error %>"
            });
        <% } %>

        const form = document.getElementById('loginForm');
        const usernameInput = document.getElementById('username');
        const passwordInput = document.getElementById('password');

        form.onsubmit = (e) => {
            let hasError = false;

            // รีเซ็ตสถานะ
            [usernameInput, passwordInput].forEach(input => {
                input.classList.remove('input-error');
                document.getElementById(`${input.id}-error`).classList.add('hidden');
            });

            if (!usernameInput.value.trim()) {
                usernameInput.classList.add('input-error');
                document.getElementById('username-error').classList.remove('hidden');
                hasError = true;
            }

            if (!passwordInput.value.trim()) {
                passwordInput.classList.add('input-error');
                document.getElementById('password-error').classList.remove('hidden');
                hasError = true;
            }

            if (hasError) {
                e.preventDefault();
                Toast.fire({
                    icon: "warning",
                    title: "กรุณากรอกข้อมูลให้ครบถ้วน"
                });
            }
        };

        // ล้าง error เมื่อมีการพิมพ์
        [usernameInput, passwordInput].forEach(input => {
            input.oninput = () => {
                input.classList.remove('input-error');
                document.getElementById(`${input.id}-error`).classList.add('hidden');
            };
        });

        // PDPA Consent Check
        function checkPDPA() {
            const hasConsented = localStorage.getItem('pdpa_consent_v1');
            if (!hasConsented) {
                Swal.fire({
                    title: 'นโยบายความเป็นส่วนตัว (PDPA)',
                    html: `
                        <div class="text-left py-2">
                            <p class="mb-4 text-slate-600 leading-relaxed">เพื่อให้สอดคล้องกับพระราชบัญญัติคุ้มครองข้อมูลส่วนบุคคล (PDPA) ระบบขอแจ้งรายละเอียดการจัดเก็บและประมวลผลข้อมูลสำคัญดังนี้:</p>
                            <div class="space-y-3 mb-6">
                                <div class="p-3 bg-indigo-50 rounded-xl border border-indigo-100">
                                    <p class="text-sm font-bold text-indigo-800 mb-1"><i class="fas fa-file-shield mr-2"></i>การจัดเก็บข้อมูล</p>
                                    <p class="text-xs text-indigo-700">ระบบจะจัดเก็บไฟล์ภาพ (สลิป/บิล) และข้อมูลตัวเลขที่อ่านได้เข้าสู่ Cloud Storage ที่มีความปลอดภัยสูง เพื่อใช้ในการประมวลผลตามคำสั่งของท่าน</p>
                                </div>
                                <div class="p-3 bg-blue-50 rounded-xl border border-blue-100">
                                    <p class="text-sm font-bold text-blue-800 mb-1"><i class="fas fa-microchip mr-2"></i>วัตถุประสงค์การใช้ข้อมูล</p>
                                    <p class="text-xs text-blue-700">ใช้เทคโนโลยี AI เพื่ออ่านข้อมูล (OCR) สำหรับการคำนวณรายรับ-รายจ่าย, สรุปผลทางบัญชี, และวิเคราะห์ข้อมูลในแบบภาพรวม (Dashboard) เท่านั้น</p>
                                </div>
                                <div class="p-3 bg-amber-50 rounded-xl border border-amber-100">
                                    <p class="text-sm font-bold text-amber-800 mb-1"><i class="fas fa-user-lock mr-2"></i>การรักษาความลับ</p>
                                    <p class="text-xs text-amber-700">ข้อมูลของท่านจะถูกเก็บเป็นความลับสูงสุด ไม่มีการเปิดเผยต่อบุคคลภายนอก และท่านสามารถแจ้งยกเลิกความยินยอมได้ทุกเมื่อผ่านช่องทางติดต่อผู้ดูแลระบบ</p>
                                </div>
                            </div>
                            <div class="flex items-center gap-3 p-4 bg-slate-100 rounded-2xl border border-slate-200">
                                <input type="checkbox" id="pdpaCheckbox" class="w-5 h-5 accent-indigo-500 cursor-pointer">
                                <label for="pdpaCheckbox" class="text-sm font-bold text-slate-700 cursor-pointer">ฉันได้อ่านและยอมรับเงื่อนไขการประมวลผลข้อมูลส่วนบุคคล</label>
                            </div>
                        </div>
                    `,
                    icon: 'info',
                    confirmButtonText: 'ยอมรับและดำเนินการต่อ',
                    confirmButtonColor: '#6366f1',
                    allowOutsideClick: false,
                    allowEscapeKey: false,
                    customClass: {
                        popup: 'rounded-3xl',
                        confirmButton: 'px-8 py-3 rounded-xl font-bold'
                    },
                    preConfirm: () => {
                        const checkbox = document.getElementById('pdpaCheckbox');
                        if (!checkbox.checked) {
                            Swal.showValidationMessage('กรุณาติ๊กยอมรับเงื่อนไขก่อนดำเนินการต่อ');
                            return false;
                        }
                        return true;
                    }
                }).then((result) => {
                    if (result.isConfirmed) {
                        const consentDate = new Date().toISOString();
                        localStorage.setItem('pdpa_consent_v1', consentDate);

                        // บันทึกไปยัง Server (JSON File)
                        fetch('/api/pdpa/consent', {
                            method: 'POST',
                            headers: { 'Content-Type': 'application/json' },
                            body: JSON.stringify({
                                consent_value: 'accepted',
                                user_agent: navigator.userAgent
                            })
                        }).catch(err => console.error('Failed to sync consent:', err));
                    }
                });
            }
        }

        // Call PDPA check on load
        window.addEventListener('DOMContentLoaded', checkPDPA);
    </script>
</body>

</html>

</html>