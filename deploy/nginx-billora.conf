server {
    listen 80;
    server_name YOUR_DOMAIN_NAME.com www.YOUR_DOMAIN_NAME.com;

    # Redirect all HTTP traffic to HTTPS (will be handled by Certbot later)
    # But for now, we proxy strictly or just prepare the block
    
    location / {
        proxy_pass http://localhost:3000; # Port that your Node.js app runs on
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
