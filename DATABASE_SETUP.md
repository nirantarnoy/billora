# 🚀 วิธีติดตั้งฐานข้อมูล - ง่ายมาก!

## ✅ วิธีที่ 1: ใช้ npm script (แนะนำ)

```bash
npm run setup-db
```

**ง่ายที่สุด!** รันคำสั่งเดียวเสร็จ

---

## ✅ วิธีที่ 2: ใช้ Batch File (Windows)

**ดับเบิลคลิก** ที่ไฟล์ `setup-db.bat` หรือรันใน terminal:

```bash
.\setup-db.bat
```

---

## ✅ วิธีที่ 3: ใช้ PowerShell Script (Windows)

```powershell
.\setup-db.ps1
```

หรือถ้าเจอ error เรื่อง execution policy:

```powershell
powershell -ExecutionPolicy Bypass -File .\setup-db.ps1
```

---

## ✅ วิธีที่ 4: รันด้วย Node.js โดยตรง

```bash
node database/setup-database.js
```

---

## 📋 สิ่งที่ Script จะทำ

1. ✅ เชื่อมต่อฐานข้อมูล
2. ✅ สร้างตาราง `tenants` (องค์กร/บริษัท)
3. ✅ สร้างตาราง `users` (ผู้ใช้แบบ multi-tenant)
4. ✅ สร้างตาราง `subscription_plans` (แพ็กเกจ 4 แบบ)
5. ✅ สร้างตาราง `tenant_subscriptions` (ประวัติการชำระเงิน)
6. ✅ ตรวจสอบว่าตารางถูกสร้างครบหรือไม่
7. ✅ แสดงสถิติฐานข้อมูล

---

## 🎯 ตัวอย่างผลลัพธ์

```
============================================================
  🚀 Billora Multi-tenant Database Setup
============================================================

ℹ เชื่อมต่อฐานข้อมูล...
✓ เชื่อมต่อฐานข้อมูล bill_ocr สำเร็จ

▶ สร้างตาราง Tenants (องค์กร/บริษัท)...
✓ สร้างตาราง Tenants (องค์กร/บริษัท) - สำเร็จ

▶ สร้างตาราง Users (Multi-tenant)...
✓ สร้างตาราง Users (Multi-tenant) - สำเร็จ

▶ สร้างตาราง Subscription Plans และ History...
✓ สร้างตาราง Subscription Plans และ History - สำเร็จ

▶ ตรวจสอบตารางที่สร้าง...
✓ ตารางทั้งหมดถูกสร้างเรียบร้อย

------------------------------------------------------------
ℹ สถิติฐานข้อมูล:
  • tenants: 0 แถว
  • users: 0 แถว
  • subscription_plans: 4 แถว
  • tenant_subscriptions: 0 แถว
------------------------------------------------------------

✓ 🎉 ติดตั้งฐานข้อมูลสำเร็จ!

ℹ ขั้นตอนถัดไป:
  1. Restart server: nodemon .\server.js
  2. ทดสอบ API: ดูไฟล์ API_TESTING.http
  3. อ่านคู่มือ: START_HERE.md
```

---

## ⚠️ ถ้าเจอปัญหา

### ปัญหา: ER_ACCESS_DENIED_ERROR
```
✗ ไม่สามารถเชื่อมต่อฐานข้อมูลได้ - ตรวจสอบ username/password ใน .env
```

**แก้ไข**: ตรวจสอบไฟล์ `.env` ว่า DB_USER และ DB_PASSWORD ถูกต้อง

### ปัญหา: ER_BAD_DB_ERROR
```
✗ ไม่พบฐานข้อมูล bill_ocr - กรุณาสร้างฐานข้อมูลก่อน
```

**แก้ไข**: สร้างฐานข้อมูลก่อน
```sql
CREATE DATABASE bill_ocr CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

### ปัญหา: ตารางมีอยู่แล้ว
```
⚠ สร้างตาราง Tenants - ตารางมีอยู่แล้ว (ข้าม)
```

**ไม่ต้องกังวล**: Script จะข้ามตารางที่มีอยู่แล้วอัตโนมัติ

---

## 🔧 การตั้งค่า .env

ตรวจสอบว่าไฟล์ `.env` มีค่าเหล่านี้:

```env
# Database
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=bill_ocr

# JWT
JWT_SECRET=billora-jwt-secret-key-2026
```

---

## 📝 หมายเหตุ

- ✅ Script จะไม่ลบข้อมูลเดิม
- ✅ ถ้าตารางมีอยู่แล้ว จะข้ามไป
- ✅ ปลอดภัย รันได้หลายครั้ง
- ✅ แสดงสถิติหลังติดตั้งเสร็จ

---

## 🎓 ขั้นตอนถัดไป

หลังจากติดตั้งฐานข้อมูลเสร็จแล้ว:

1. **Restart Server**
   ```bash
   nodemon .\server.js
   ```

2. **ทดสอบ API**
   - เปิดไฟล์ `API_TESTING.http`
   - ทดสอบ Registration และ Login

3. **อ่านคู่มือ**
   - `START_HERE.md` - เริ่มต้นที่นี่
   - `INSTALLATION_STEPS.md` - ขั้นตอนถัดไป

---

**ง่ายมาก! แค่รันคำสั่งเดียว:** `npm run setup-db` 🚀
